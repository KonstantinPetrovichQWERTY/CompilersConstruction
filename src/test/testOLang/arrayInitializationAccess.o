class Main is
    this() is
        var arr : Array[Integer](5)
        arr.set(0, Integer(10))
        arr.set(1, Integer(20))
        arr.set(2, Integer(30))

        var val0 : Integer(0)
        val0 := arr.get(0)
        val0.print()

        var len : Integer(0)
        len := arr.Length()
        len.print()

        var list : List[Integer]()
        list := arr.toList()
        list.print()
    end
end
