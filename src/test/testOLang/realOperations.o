class Main is
    this() is
        var a : Real(2.2)
        var b : Real(100)

        var min : Real(0)
        min.getMin().print()
        var max : Real(0)
        max.getMax().print()
        var epsilon : Real(0)
        max.getEpsilon().print()

        var resultReal : Real(0.0)
        resultReal := a.toInteger()
        resultReal.print()

        b.UnatyMinus().print()

        var resultPlus : Real(0.0)
        resultPlus := a.Plus(b)
        resultPlus.print()

        var resultMinus : Real(0.0)
        resultMinus := a.Minus(b)
        resultMinus.print()

        var resultMult : Real(0.0)
        resultMult := a.Mult(b)
        resultMult.print()

        var resultDiv : Real(0.0)
        resultDiv := a.Div(b)
        resultDiv.print()

        var resultDiv : Real(0.0)
        resultDiv := a.Div(b)
        resultDiv.print()

        // остаток от деления
        var resultRem : Real(0.0)
        resultRem := a.Rem(b)
        resultRem.print()

        a.Less(b).print()
        a.LessEqual(b).print()
        a.Greater(b).print()
        a.GreaterEqual(b).print()
        a.Equal(b).print()
    end
end
